cmake_minimum_required(VERSION 3.8)
project(agt_inspection_exporter)
find_package(ament_cmake REQUIRED)
find_package(ament_cmake_python REQUIRED)
ament_python_install_package(${PROJECT_NAME})
install(PROGRAMS scripts/export_inspection_run.py scripts/validate_inspection_dataset.py DESTINATION lib/${PROJECT_NAME})
if(BUILD_TESTING)
  find_package(ament_cmake_pytest REQUIRED)
  ament_add_pytest_test(test_inspection_dataset test/test_inspection_dataset.py)
endif()
ament_package()
